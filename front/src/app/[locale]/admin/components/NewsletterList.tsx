"use client";

import React from "react";
import { NewsletterListContainer } from "./NewsletterList.sty";

const NewsletterList = () => {
  return <NewsletterListContainer>a</NewsletterListContainer>;
};

export default NewsletterList;
